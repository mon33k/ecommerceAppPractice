import React from 'react';

const LoginError = ({message}) => {

    return(
        <div>
            <h4>LOGIN ERROR PAGE</h4>
            <p>{message}</p>
        </div>
    )
}

export default LoginError